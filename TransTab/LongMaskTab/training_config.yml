output_folder: checkpoint

train:
  batch_size: 256
  epochs: 500
  patience: 30
  learning_rate: 0.00001
  weight_decay: 0.0001
  dropout: 0.5
  use_CLIP: true
  use_NaN_embedding: false
  fast_training_for_test: false
  device: 0